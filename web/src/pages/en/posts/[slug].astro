---
import PostDetailPage from '@/components/pages/PostDetailPage.astro';
import { getPostDetailOrNull } from '@/lib/api';

const detail = await getPostDetailOrNull('en', Astro.params.slug!);
if (!detail) {
  return Astro.rewrite('/404');
}
---

<PostDetailPage lang="en" detail={detail} />
